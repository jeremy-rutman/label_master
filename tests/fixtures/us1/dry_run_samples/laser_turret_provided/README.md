# Laser Turret Provided Dataset Manifests

These manifests are bound to user-provided datasets under:

`/home/jeremy/data/drone_detect/laser_turret/yolo_models/datasets/`

They use absolute `dataset_root` values for local dry-run validation.

If you move the datasets, update each `dataset_root` path.
